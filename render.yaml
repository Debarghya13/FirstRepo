services:
  - type: web
    name: smart-trading-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run share_market_test.py --server.port 10000 --server.address 0.0.0.0
    plan: free
